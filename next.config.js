/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['platform.theverge.com',
    'cdn.vox-cdn.com'],
   
  },
};

module.exports = nextConfig;
